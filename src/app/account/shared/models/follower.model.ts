export class Follower {
    avatar: string;
    id: number;
    username: string;
    constructor(
        avatar: string,
        username: string,
        ) {
            this.avatar = avatar;
            this.username = username;
        }
}
